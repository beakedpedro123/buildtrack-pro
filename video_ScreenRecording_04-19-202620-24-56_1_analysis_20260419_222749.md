Here is a description of the interactions shown in the video:

**Compass Button Behavior:**
When the user taps the "Compass" button (labeled "Direction & Navigation") located under the "Tools" section on the clock screen, the application switches and opens the **Google Maps** app. It initially displays the user's saved lists and then transitions to a map view of a specific area.

**Job Selector Dropdown:**
The job selector is located above the green "Clock In" button. 
*   **Appearance:** It features a header button that says "10 sites" with an upward-pointing arrow, indicating that the list is currently expanded. Below this header is a list of job site names (e.g., "England rem...", "Hyde", "Morgan D12", "Rickys deck"), each with a circular radio button next to it for selection.
*   **Behavior:** It functions as an expanded, scrollable list. The user can scroll through the available job sites and tap a radio button to select a specific job before clocking in. The upward arrow suggests the list can likely be collapsed by tapping the "10 sites" header.