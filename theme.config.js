/** @type {const} */
const themeColors = {
  // Gold accent — the brand's signature color
  primary:    { light: '#C9A84C', dark: '#D4AF37' },
  // Page backgrounds
  background: { light: '#FFFFFF', dark: '#0D0D0D' },
  // Card / elevated surface
  surface:    { light: '#F5F5F0', dark: '#1A1A1A' },
  // Primary text
  foreground: { light: '#111111', dark: '#F0EDE6' },
  // Secondary / muted text
  muted:      { light: '#6B6B6B', dark: '#888880' },
  // Borders and dividers
  border:     { light: '#E0DDD5', dark: '#2E2E2E' },
  // Status colors
  success:    { light: '#2D7A4F', dark: '#4ADE80' },
  warning:    { light: '#C9A84C', dark: '#D4AF37' },
  error:      { light: '#C0392B', dark: '#F87171' },
  // Gold tint (alias for tint usage in tab bar)
  tint:       { light: '#C9A84C', dark: '#D4AF37' },
};

module.exports = { themeColors };
